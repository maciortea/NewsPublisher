@model IEnumerable<Web.Models.ArticleViewModel>

@{
    ViewData["Title"] = "Articles";
}

<h1>@ViewData["Title"]</h1>

<table class="table">
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.ActionLink(item.Title, "Article", new {  id = item.Id }, new { @class = "h5 btn-link" })
                </td>
            </tr>
        }
    </tbody>
</table>
